<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= page_title %></title>
  
  <!-- Bootstrap 5 CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  
  <!-- Font Awesome -->
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
  
  <style>
    /* Modern System Font Stack */
    body { 
      font-family: -apple-system, BlinkMacSystemFont, "Avenir Next", avenir, "Segoe UI", "Helvetica Neue", helvetica, Ubuntu, roboto, noto, arial, sans-serif;
    }
    
    /* Google Search Results Wrapper */
    .search-results {
      font-family: arial, sans-serif; /* Google's exact font for search results */
    }
    
    /* Exact Google SERP Styles */
    .search-results .result-title { 
      font-size: 20px; 
      color: #1a0dab; 
      line-height: 1.3;
    }
    .search-results .result-title:hover { 
      text-decoration: underline; 
      color: #1a0dab;
    }
    .search-results .result-url { 
      font-size: 14px; 
      color: #006621; 
      line-height: 1.3;
    }
    .search-results .result-description { 
      font-size: 14px; 
      color: #4d5156; 
      line-height: 1.58;
    }
    .search-results .result-meta { 
      font-size: 12px; 
      color: #70757a; 
    }
    .search-results .result-site { 
      font-size: 14px; 
      color: #202124; 
    }
    
    /* Google-style image with grey outline and curves */
    .result-image {
      border: 1px solid #dadce0;
      border-radius: 8px;
      object-fit: cover;
    }
    
    /* Minimal custom CSS - use Bootstrap classes instead */
  </style>
</head>
<body>
  
  <!-- Bootstrap 5 Navbar -->
  <nav class="navbar navbar-expand-lg navbar-light bg-light border-bottom">
    <div class="container-fluid">
      <a class="navbar-brand fw-bold" href="https://nimbus.ai"><i class="fas fa-bullseye me-1"></i>Nimbus AI</a>
    </div>
  </nav>

  <div class="container">
    
    <!-- Bootstrap 5 Jumbotron -->
    <div class="bg-primary text-white rounded-3 p-5 my-4">
      <div class="container-fluid py-2">
        <h1 class="fs-4 fw-semibold">Optimization Results...</h1>
        
        <!-- Stats in Jumbotron -->
        <div class="row g-4 my-3">
          <div class="col-6 col-md-3 text-center">
            <div class="display-4 fw-bold"><%= pages_count %></div>
            <div class="fs-6 opacity-75">Pages Optimized</div>
          </div>
          <div class="col-6 col-md-3 text-center">
            <div class="display-4 fw-bold"><%= avg_confidence %>%</div>
            <div class="fs-6 opacity-75">Avg Confidence</div>
          </div>
          <div class="col-6 col-md-3 text-center">
            <div class="display-4 fw-bold"><%= total_changes %></div>
            <div class="fs-6 opacity-75">Total Changes</div>
          </div>
          <% if (avg_time) { %>
          <div class="col-6 col-md-3 text-center">
            <div class="display-4 fw-bold"><%= avg_time %>ms</div>
            <div class="fs-6 opacity-75">Avg Speed</div>
          </div>
          <% } %>
        </div>
        <hr>
        <% if (business_goal) { %>
        <p class="fs-6 mb-0 opacity-75"><i class="fas fa-bullseye me-1"></i><strong class="fw-semibold">Goal:</strong> <%= business_goal %></p>
        <% } %>
      </div>
    </div>

    <!-- Explanatory Text -->
    <div class="alert alert-info border-0 bg-light mb-4">
      <p class="mb-0 text-muted">
        <i class="fas fa-search me-2"></i>
        <strong>Preview:</strong> This is how your optimized pages will appear in Google search results
      </p>
    </div>

    <!-- Search Results -->
    <main class="search-results">
      <% results.forEach(function(result) { %>
      <article class="mb-4">
        <div class="d-flex gap-3 align-items-start">
          <div class="flex-grow-1" style="min-width: 0;">
            
            <!-- Site Info (Google style) -->
            <div class="result-site d-flex align-items-center mb-1">
              <img src="<%= favicon_url %>" 
                   alt="<%= business_name %> favicon"
                   width="16" height="16" 
                   class="me-2"
                   onerror="this.src='data:image/svg+xml,<svg xmlns=&quot;http://www.w3.org/2000/svg&quot; viewBox=&quot;0 0 16 16&quot;><circle cx=&quot;8&quot; cy=&quot;8&quot; r=&quot;6&quot; fill=&quot;%231a73e8&quot;/></svg>'">
              <span><%= business_name %></span>
            </div>
            
            <!-- URL Breadcrumb (Google style) -->
            <div class="result-url mb-1">
              <%= result.url_display %>
            </div>
            
            <!-- Title (Google style) -->
            <a href="<%= result.page_id %>.html" class="result-title text-decoration-none d-block mb-1">
              <%= result.optimized_title %>
              <% if (result.title_char_count) { %>
              <span class="small text-<%= result.title_char_class === 'char-good' ? 'success' : result.title_char_class === 'char-warn' ? 'warning' : 'danger' %>">
                <%= result.title_char_count %>/60
              </span>
              <% } %>
            </a>
            
            <!-- Description (Google style) -->
            <p class="result-description mb-2">
              <%= result.optimized_description %>
              <% if (result.desc_char_count) { %>
              <span class="small text-<%= result.desc_char_class === 'char-good' ? 'success' : result.desc_char_class === 'char-warn' ? 'warning' : 'danger' %>">
                <%= result.desc_char_count %>/160
              </span>
              <% } %>
            </p>
            
            <!-- Meta Information -->
            <div class="result-meta d-flex gap-3 flex-wrap align-items-center">
              <span class="badge bg-light text-dark">
                <%= result.cache_status %> Results
              </span>
              <span class="text-<%= result.confidence >= 90 ? 'success' : result.confidence >= 80 ? 'warning' : 'danger' %>">
                <%= result.confidence %>% confidence
              </span>
              <span class="text-muted">
                <%= result.changes_count %> changes
              </span>
              <% if (result.tone) { %>
              <span class="text-muted">
                <i class="fas fa-microphone me-1"></i><%= result.tone %> tone
              </span>
              <% } %>
              <% if (result.tier) { %>
              <span class="text-muted">
                <i class="fas fa-layer-group me-1"></i>Tier <%= result.tier %>
              </span>
              <% } %>
            </div>
            
          </div>
          
          <!-- Result Image (Google style - right floated) -->
          <% if (result.result_image) { %>
          <div class="flex-shrink-0">
            <img src="<%= result.result_image %>" 
                 alt="<%= result.page_id %> preview" 
                 width="120" height="120"
                 class="result-image"
                 onerror="this.style.display='none'">
          </div>
          <% } %>
          
        </div>
      </article>
      <% }); %>
    </main>

    
  </div>

  <!-- Bootstrap 5 Footer -->
  <footer class="bg-light border-top mt-5 py-4">
    <div class="container">
      <div class="row align-items-center">
        <div class="col-md-6">
          <p class="mb-0 text-muted">
            🎯 <%= optimization_type %> • Generated by 
            <a href="https://nimbus.ai" class="text-decoration-none" target="_blank">Nimbus AI</a>
          </p>
        </div>
        <div class="col-md-6 text-md-end">
          <p class="mb-0 text-muted small">
            Report generated: <%= generated_at %>
          </p>
        </div>
      </div>
    </div>
  </footer>

  <!-- Bootstrap 5 JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
